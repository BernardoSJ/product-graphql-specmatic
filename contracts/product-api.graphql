enum ProductType {
  gadget
  book
  food
  other
}

type Query {
  findAvailableProducts(type: ProductType!, pageSize: Int): [Product]
  findProductById(id: ID): Product
}

type Product {
  id: ID!
  name: String!
  inventory: Int!
  type: ProductType!
}